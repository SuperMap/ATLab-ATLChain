/*
 *  Tool set for app
 *  
 *  Author: cystone@aliyun.com
 *  Date:   2019-01-08
 *  Log:    create
 *
 */


// TODO: checkCert(cert, username, orgname)
function checkCert(cert, username, orgName) {
    
    return true;
}

// TODO: signatureVerify(signature)
function signatureVerify(cert, signature) {
    var pubKey = getPubKeyFromCert(cert);

    return true;
}

function getKeyFromCert(Cert) {

    return pubKey;
}
